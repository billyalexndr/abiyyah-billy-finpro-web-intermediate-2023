import { Lato, Arvo } from "next/font/google";

export const lato = Lato({
    weight: "400",
    subsets: ["latin"],
    display: "swap",
});

export const arvo = Arvo({
    weight: "400",
    subsets: ["latin"],
    display: "swap",
});
